"""Names shared by the desktop chain's env.py and the bootstrap that reads
its current revision — one definition, so the two can never disagree."""

SCHEMA = "desktop"
VERSION_TABLE = "alembic_version_desktop"
