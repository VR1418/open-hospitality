# `vite build --mode desktop` (npm run build:desktop): the desktop edition's
# portal. Sessions come from the launcher, not an OIDC provider — see
# src/auth/oidc.ts and docs/desktop/.
VITE_AUTH_MODE=desktop
